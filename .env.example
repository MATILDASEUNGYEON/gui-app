# 이 파일을 복사해 `.env.local` 로 만들고 실제 값을 채우세요.
#   cp .env.example .env.local
# (.env.local 은 .gitignore 로 커밋되지 않습니다)

# GrapesJS Studio SDK 라이선스 키.
# - localhost 접속만 한다면 비워둬도 됩니다(DEV_LICENSE_KEY로 자동 폴백).
# - 외부 도메인/IP로 접속하려면 app.grapesjs.com 에서 발급받은 키가 필요합니다.
VITE_GRAPESJS_LICENSE_KEY=
